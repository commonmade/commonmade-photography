rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Anyone can read (so the public website works)
      allow read: if true;
      // Only authenticated users (the admin) can write
      allow write: if request.auth != null;
    }
  }
}
